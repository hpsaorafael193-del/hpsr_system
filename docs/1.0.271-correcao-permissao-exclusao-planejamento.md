# v1.0.271 — Correção de permissão ao excluir planejamento

- Corrige a divergência entre a autorização exibida pela interface e a função `is_hpsr_schedule_manager()` do banco.
- Direção e Diretor Técnico / Dev passam a ser reconhecidos também pelos dados administrativos de `team_members`, incluindo `payload.systemRole`.
- A hierarquia existente não foi ampliada.
- A correção afeta as RPCs e políticas que já reutilizam `is_hpsr_schedule_manager()`, incluindo exclusão de planejamentos e agendas publicadas.
