# v1.0.350 — Correção de build

## Correções
- Restaura `VALID_PATIENT_FOLLOW_UP` em Prontuários (`Rotina`, `Clínico`, `Especializado`).
- Fixa `es-to-primitive` em `1.3.0` e adiciona override do pnpm para que dependências transitivas usem a mesma versão.
- O objetivo do pin é garantir o submódulo `es-to-primitive/es2015` exigido pela cadeia ESLint/es-abstract.

## Banco
Nenhuma migration ou alteração de banco nesta versão. A Mudança 6 permanece como aplicada na v1.0.349.
