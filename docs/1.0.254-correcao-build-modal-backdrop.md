# HPSR 1.0.254 — Correção de build do backdrop dos modais

## Ajuste
- Corrigida a utility Tailwind inválida `bg-[#1f0805]/62` usada dentro de `@apply` em `src/app/globals.css`.
- Mantidos exatamente a cor `#1f0805` e 62% de opacidade usando CSS nativo: `background-color: rgb(31 8 5 / 0.62)`.
- Nenhuma regra de negócio, fluxo, consulta, migration ou outra otimização da v1.0.253 foi alterada.

## Banco
- Nenhuma migration.
