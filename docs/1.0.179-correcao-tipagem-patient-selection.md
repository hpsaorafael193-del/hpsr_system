# Versão 1.0.179 — Correção de tipagem no seletor de pacientes

## Objetivo
Corrigir o erro de TypeScript que impedia o build da versão 1.0.178.

## Alteração realizada
- Criado o tipo `PatientRegistryRow` para representar o retorno da RPC `list_patient_registry_staff`.
- O parâmetro `row` do mapeamento agora possui tipagem explícita.
- Nenhuma regra funcional, consulta ou comportamento foi alterado.

## Migração
- Nenhuma nova migração.
- Continua necessária a migração da versão 1.0.178:
  - `supabase/migrations/20260803_000043_stabilize_patient_registry_and_time_clock.sql`

## Validação
- Arquivos JSON de versão validados.
- Revisão estática da tipagem aplicada.
- O build completo não pôde ser repetido neste ambiente porque o pacote limpo não contém `node_modules`.
