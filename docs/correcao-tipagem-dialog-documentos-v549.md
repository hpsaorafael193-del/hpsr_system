# v0.5.49 — Correção de tipagem no diálogo de pré-visualização

- Removida a propriedade `tone` do diálogo da aba Documentos, pois o tipo local `AppDialog` não aceita esse campo.
- A mensagem de erro e as ações do diálogo foram preservadas.
- Corrige a falha de build apontada em `src/app/dashboard/documentos/page.tsx`.
