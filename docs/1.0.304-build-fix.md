# v1.0.304 — Correção de build

- Corrige a importação de `normalizeSpecialty` na Agenda Clínica, reutilizando a função já existente em `src/data/appointment-rules.ts`.
- Mantém `es-to-primitive@1.3.4` como dependência direta de desenvolvimento.
- Adiciona `.npmrc` com hoist público restrito a `es-to-primitive` para reconstrução correta do link no `node_modules` do pnpm.
- Não altera banco, regras de agendamento ou dados clínicos.

Após substituir a versão, reconstruir as dependências para eliminar links antigos do pnpm:

```powershell
Remove-Item -Recurse -Force node_modules
pnpm install --force
pnpm run build
```
