# Versão 1.0.192 — Correção de build na Agenda Clínica

## Correção
- Adicionado o import de `normalizeClinicalPassport` em `src/app/dashboard/agendamento/clinica/page.tsx`.
- Reutilizada a função já existente em `src/lib/clinical-scheduling.ts`.
- Nenhuma regra de negócio ou consulta foi alterada.

## Migrações
- Nenhuma.

## Validação
- Confirmada a exportação da função no módulo clínico.
- Confirmado o uso do import no arquivo da Agenda Clínica.
- Build completo não executado neste ambiente por ausência das dependências instaladas.
