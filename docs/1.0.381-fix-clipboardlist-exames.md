# HPSR System 1.0.381

Correção pontual de runtime na página de Exames.

- Adicionado `ClipboardList` ao import de `lucide-react` em `src/app/dashboard/exames/page.tsx`.
- Corrige `ReferenceError: ClipboardList is not defined` ao renderizar o bloco **Informações do exame**.
- Nenhuma alteração de layout, regra clínica, Supabase ou banco de dados nesta versão.
