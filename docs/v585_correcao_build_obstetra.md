# v0.5.85 — Correção de build da aba Obstetra

- Restaurado o componente `Field`.
- Restaurado o `export default` da página Obstetra.
- Reorganizada a estrutura do componente para que cálculo, estados e renderização permaneçam dentro da página correta.
- Corrigido o erro de tipagem do Next.js que informava ausência da exportação padrão.
