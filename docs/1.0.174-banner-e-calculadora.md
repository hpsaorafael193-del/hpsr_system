# Versão 1.0.174 — Banner institucional e ajuste da calculadora

## Objetivo
Atualizar a imagem principal do sistema e reorganizar dois itens entre as categorias da calculadora.

## Arquivos alterados
- `public/hospital-sao-rafael-banner.webp`
- `src/app/dashboard/calculadora/page.tsx`
- `src/components/layout/DeveloperCreditsModal.tsx`
- `package.json`
- `package-lock.json`

## Alterações realizadas
- Substituída a imagem `hospital-sao-rafael-banner.webp` pela nova arte enviada.
- A nova imagem passa a ser usada automaticamente:
  - no banner da página inicial;
  - no banner principal da página inicial do dashboard.
- Na calculadora:
  - `REMÉDIOS` foi movido para `Itens médicos`;
  - `URSOS MÉDICOS` foi movido para `Serviços Médicos`.
- A troca preserva nomes, imagens e valores de cada item.

## Migrações
- Nenhuma.

## Otimizações
- A nova imagem foi mantida em WebP para evitar aumento desnecessário do peso da página.
- Nenhuma consulta nova ao Supabase.

## Validações
- Revisão estática dos arquivos alterados.
- Conferência das referências de imagem e da organização das categorias.
- Build completo não executado no ambiente por ausência das dependências instaladas.
