# HPSR v1.0.289 — sincronização de tipagem do rodapé público

- Explicita `DeveloperCreditsModalProps` no componente compartilhado de créditos/suporte.
- Exporta e reutiliza `SupportContext` no `PublicSystemFooter`, evitando contratos duplicados.
- Garante que `showSupport`, `supportContext` e `supportFirst` fazem parte da assinatura pública do modal.
- Base integral: v1.0.288. Nenhuma alteração de banco ou fluxo clínico.
- Build completo deve ser validado após reinstalação limpa das dependências.
