# HPSR v1.0.296 — Correção de build

Correção incremental sobre a v1.0.295 após execução real de `pnpm run build` em ambiente local.

## Ajustes

- `UserMenu.tsx`: normalização explícita de `payload.specialty` para `string` antes da renderização, removendo o erro TypeScript `unknown is not assignable to ReactNode`.
- `es-to-primitive@1.3.4`: promovido a `devDependency` direta para estabilizar a resolução usada por `eslint-plugin-react` / `es-abstract` no pnpm.
- `package.json`, `package-lock.json`, `pnpm-lock.yaml` e versão visível do sistema atualizados para `1.0.296`.

## Banco de dados

Nenhuma nova alteração no Supabase nesta versão. As migrations da v1.0.295 permanecem válidas.

## Validação

O ambiente de empacotamento não conseguiu instalar dependências porque `registry.npmjs.org` retornou `EAI_AGAIN`. Portanto, o build completo da v1.0.296 deve ser confirmado no ambiente local com dependências instaladas.
